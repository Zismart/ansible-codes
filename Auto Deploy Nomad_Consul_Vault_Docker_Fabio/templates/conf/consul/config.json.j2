 {
     "advertise_addr": "{{ consul_advertise_addr }}",
     "bind_addr": "{{ consul_bind_addr }}",
     "bootstrap_expect": {{ consul_bootstrap_expect }},
     "client_addr": "{{ consul_client_addr }}",
     "datacenter": "{{ consul_datacenter }}",
     "data_dir": "{{ consul_data_dir }}",
     "domain": "{{ consul_domain }}",
     "enable_script_checks": {{ consul_enable_script_checks | lower }},
     "server": {{ consul_server | lower }},
     "retry_join": ["{{ consul_retry_join }}"],
     "dns_config": {
         "enable_truncate": {{ consul_enable_truncate | lower }},
         "only_passing": {{ consul_only_passing | lower }}
     },
     "enable_syslog": {{ consul_enable_syslog | lower }},
     "encrypt": "{{ consul_encrypt_key.stdout }}",
     "leave_on_terminate": {{ consul_leave_on_terminate | lower }},
     "log_level": "{{ consul_log_level }}",
     "rejoin_after_leave": {{ consul_rejoin_after_leave | lower }},
     "ui": {{ consul_ui | lower }}
 }
